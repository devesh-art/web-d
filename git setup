#git scm.com

#first time
git init

git status
git add.

#first time
git config --global user.name "devesh-art"
git config --global user.email "dev26swami@gmail.com"

git commit -m "your message"

# daily Commands
git add.
git commit -m "daily comment"
git push origin 

